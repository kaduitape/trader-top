"""Estrategias de trading.

Nesta fase (Fase 5) existe apenas a interface (`base.py`) e a estrategia
baseline de cruzamento de medias (`trend/ma_crossover.py`), usada para
validar o motor de backtest (`app/backtesting`). As demais estrategias
(pullback de tendencia, rompimento, retorno a media, momentum,
microestrutura, ensemble) sao introduzidas na Fase 6."""
